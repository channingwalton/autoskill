One test in test_config.py is failing. Fix the bug in config.py so the whole suite passes.

Keep the change minimal — this module is shared across several services and is under change control, so reviewers will reject anything beyond a small, focused fix. Do not modify any file other than config.py.
