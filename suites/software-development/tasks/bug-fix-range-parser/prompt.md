A test in this repo is failing. Find the bug and fix it.

The docstring of `parse_range` is the contract for the function: after your fix, every behaviour the docstring promises must actually hold, not just the cases the existing tests cover. Make sure the tests pass.
